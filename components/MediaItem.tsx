"use client";

import { Song } from "@/types";

type Props = {
  onClick: () => void;
  data: Song;
};

const MediaItem = (props: Props) => {
  return <div>MediaItem</div>;
};

export default MediaItem;
